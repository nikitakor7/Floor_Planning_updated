#ifndef moves_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define moves_H

#include<iostream>
#include<stack>
#include<string>
#include <typeinfo>
#include<stdlib.h>

using namespace std;

string moves(string);
extern string funny;

#endif
